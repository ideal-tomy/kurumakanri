# 車検通知管理システム 技術要件

## 1. システム構成方針
- フロントエンド、バックエンドAPI、DB、ジョブワーカーを分離した構成とする。
- 通知配信（LINE/メール）は外部API連携として実装し、配信結果を内部DBへ記録する。
- AI機能はAPI経由で疎結合に実装し、将来的にモデル切替可能とする。

## 2. 推奨技術スタック
- フロントエンド: Next.js + TypeScript
- バックエンド: Node.js（NestJSまたはExpress）
- DB: PostgreSQL
- ORM: Prisma
- 非同期処理: BullMQ + Redis（またはクラウドジョブ）
- 認証/認可: JWT + RBAC（管理者/運用担当）
- 通知連携: LINE Messaging API、メール配信基盤（SendGrid/SES）
- AI連携: LLM API（文面生成、分類、品質チェック）
- 監視: アプリログ、ジョブログ、メトリクス可視化

## 3. アーキテクチャ要件
### 3.1 フロントエンド
- 画面は以下を提供すること。
  - ダッシュボード
  - 顧客一覧/詳細
  - 通知キュー
  - 配信履歴
  - テンプレート管理
- APIエラー時のユーザー通知（トースト/警告表示）を実装すること。

### 3.2 バックエンドAPI
- REST APIを提供し、JSON形式でI/Oを統一すること。
- 機能ごとにモジュール分割（customers、vehicles、notifications、templates、ai）すること。
- すべての更新系APIで監査ログを残すこと。

### 3.3 バッチ・ジョブ
- 毎日定時で通知対象抽出ジョブを実行すること。
- 配信ジョブはキュー実行し、再試行制御（最大回数、間隔）を持つこと。
- 同一通知の二重実行を防ぐため冪等キーを実装すること。

## 4. データモデル要件
## 4.1 主テーブル
- `customers`
  - id, name, furigana, phone, email, line_user_id, status, created_at, updated_at
- `vehicles`
  - id, customer_id, maker, model, plate, vin, inspection_expire_date, mileage, created_at, updated_at
- `quotes`
  - id, vehicle_id, quote_no, total_amount, status, valid_until, issued_at
- `notification_rules`
  - id, rule_name, trigger_days_before, channels, template_key, active
- `notification_jobs`
  - id, customer_id, vehicle_id, channel, template_key, scheduled_at, status, idempotency_key
- `notification_logs`
  - id, job_id, provider, provider_message_id, result, error_code, error_message, sent_at
- `consents`
  - id, customer_id, channel, opt_in, opt_out_at, source
- `template_versions`
  - id, template_key, channel, content, version, active, created_by, created_at

## 4.2 インデックス
- `vehicles(inspection_expire_date)`
- `notification_jobs(status, scheduled_at)`
- `notification_logs(job_id, sent_at)`
- `consents(customer_id, channel)`

## 5. API要件（MVP）
- `GET /api/customers`
- `GET /api/customers/:id`
- `GET /api/notifications/queue`
- `POST /api/notifications/send`
- `POST /api/notifications/batch-send`
- `GET /api/notifications/logs`
- `POST /api/templates/preview`
- `POST /api/ai/generate-message`

### 5.1 API共通仕様
- 認証必須（管理者または運用担当）。
- すべてのレスポンスにトレースIDを含める。
- バリデーションエラーは400、認証エラーは401/403、外部連携失敗は502/503を返す。

## 6. LINE連携技術要件
- Messaging APIチャネルアクセストークンを安全に保管すること。
- Push Message APIを利用し、顧客LINE userId宛に送信すること。
- Webhook受信エンドポイントを実装し、署名検証を行うこと。
- 返信イベントは非同期処理し、顧客ステータスに反映できること。

## 7. メール連携技術要件
- テンプレートベース配信（件名/本文/差し込み変数）に対応すること。
- バウンス、苦情、配信失敗をWebhookで受信し、`notification_logs` に保存すること。
- 配信停止リンクのトークン検証を実装すること。

## 8. AI連携技術要件
### 8.1 文面生成
- 入力: 顧客情報、車両情報、通知種別、テンプレートキー、トーン指定
- 出力: チャネル別文面（LINE短文、メール件名/本文）

### 8.2 品質ガード
- 禁止語、過剰表現、個人情報漏えいリスクをチェックする。
- しきい値超過時は自動送信せず「要承認」に遷移する。

### 8.3 監査性
- AI入出力（プロンプト/レスポンス要約）を監査ログに保存する。
- 機微情報はマスキングして保存する。

## 9. セキュリティ要件
- 通信はTLS必須。
- 機密情報（APIキー、トークン）はシークレットマネージャで管理する。
- DBの個人情報は暗号化または最小化ポリシーを適用する。
- 権限外アクセスを防ぐためRBACを実装する。
- 監査ログ改ざん防止（追記専用設計）を行う。

## 10. 運用・監視要件
- 送信成功率、失敗率、再送率、予約遷移率をメトリクス化する。
- ジョブ滞留、APIエラー増加時にアラート通知する。
- 障害時は再実行手順とロールバック手順を運用手順書に定義する。

## 11. 性能要件（目安）
- 通知抽出ジョブ: 10,000件を10分以内に抽出可能であること。
- 配信処理: 外部API制限内で安定送信（レート制御）できること。
- 一覧API: 95パーセンタイルで1秒以内応答を目標とする。

## 12. テスト要件
- 単体テスト: 通知判定ロジック、文面生成ロジック、バリデーション
- 結合テスト: DB更新、ジョブ投入、配信結果反映
- 外部連携テスト: LINE/メールのsandbox環境で送受信確認
- E2Eテスト: ダッシュボードから送信実行までの主要導線

## 13. リリース計画（技術）
### Phase 1
- DB構築、基本API、画面接続、手動送信

### Phase 2
- 定期ジョブ、自動通知、再送制御、配信ログ可視化

### Phase 3
- AI文面生成、品質ガード、応答分類、自動化高度化
